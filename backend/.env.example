# Example environment configuration for email/SMTP
# Copy this to .env or set as environment variables

# SMTP Configuration
# For Gmail: smtp.gmail.com, port 587
# For Mailhog (development): localhost, port 1025
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=no-reply@linkwave.app
MAIL_TLS_ENABLED=true

# Note: For Gmail, you need to create an App Password
# https://support.google.com/accounts/answer/185833

# For Mailhog (Docker development):
# MAIL_HOST=localhost
# MAIL_PORT=1025
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_TLS_ENABLED=false
